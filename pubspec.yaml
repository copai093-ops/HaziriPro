name: haziripro
version: 1.0.0+1
publish_to: 'none'
description: Attendance and workforce management app for site owners and contractors.

environment:
  sdk: '>=3.3.0 <4.0.0'

dependencies:
  flutter:
    sdk: flutter
  firebase_core: ^3.6.0
  firebase_auth: ^5.3.1
  cloud_firestore: ^5.4.4
  intl: ^0.19.0

flutter:
  uses-material-design: true
